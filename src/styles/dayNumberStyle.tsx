import { ComponentStyleConfig } from '@chakra-ui/theme';

const dayNumberStyle: ComponentStyleConfig = {
  baseStyle: {
    fontSize: 'dayNumber',
    color: 'red.main',
    textAlign: 'center',

    borderTop: '1px solid',
    borderBottom: '1px solid',
    borderColor: 'red.main',
  },
};

export default dayNumberStyle;
