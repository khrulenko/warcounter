import { ComponentStyleConfig } from '@chakra-ui/theme';

const titleStyle: ComponentStyleConfig = {
  baseStyle: {
    margin: '20px 0',

    textAlign: 'center',
    fontSize: '32px',
    color: 'white',
  },
};

export default titleStyle;
